import React from "react";

export default function DownloadButton() {
  return (
    <button
      style={{
        backgroundColor: "#2563eb",
        color: "white",
        padding: "10px 20px",
        borderRadius: "8px",
        border: "none",
        cursor: "pointer",
      }}
    >
      Download
    </button>
  );
}

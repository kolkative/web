---
const { product } = Astro.props;
---

<div class="bg-white dark:bg-gray-800 rounded-xl shadow p-4">
  <img src={product.image} alt={product.title} class="rounded-lg mb-2" />
  <h3 class="font-semibold">{product.title}</h3>
  <p class="text-gray-500 mb-3">{product.description}</p>
  <span class="font-bold text-lg">Rp {product.price.toLocaleString()}</span>
  <button class="mt-3 bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg">
    Add to Cart
  </button>
</div>
